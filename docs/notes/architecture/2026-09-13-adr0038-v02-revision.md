# ADR-0038 v0.2 定稿：按评审裁决修订并转 Accepted（#1557）

Status: implemented
Class: architecture

## Decision

- 按 9 份独立评审（issue #1557；synthesis PR #1677）与人工裁决 D-1～D-6
  （#1557 评论，2026-09-13，六项均采纳 synthesis 推荐）修订
  [`ADR-0038`](../../adr/ADR-0038-host-retirement-semantics.md) → **v0.2，转
  Accepted**；同批登记 `docs/adr/README.md` 状态行与 `docs/DOC-MAP.md`
  （补 ADR-0037/0038 两行，关闭评审 5ff80e-O9 的登记缺口）。
- 修订面：评审 R1–R19 全量映射见 ADR §6；事实性更正 6 条（F-1 前端双错、
  F-2 保留期「永久 409」、F-3 ADR-0025 死引用、F-4 后缀边界可达性、
  F-5 锚点微偏、F-6 已合销不引用）；决策改写集中在 D2（Cordon 前置）、
  D4（新列去重）、D5（14 面矩阵 + 派发归位 fatal + 回收类分类）、
  D6（身份契约 + 审计承诺降级）、新增 D8（retire ≠ credential revoke）。
- R6（`boot_id`/`agent_instance_id` 审计可见）未单列 D 项：按 synthesis 推荐
  **(a)** 采纳（承诺降级为 `audit_logs` 快照 + 当前值交付面），与 D-6 裁决
  语义相容；已在 §2 D6 与 Revisit 5 写明，如裁决层有异议可回退 (b)。
- 本 PR 纯文档，不含实现；实现单待 ADR 合入后另开（建议按 synthesis §7
  六项拆分，验收矩阵采用 182d4e-F7 的 10 场景表与逐点 mutation 纪律）。

## Alternatives

- **另开「定稿 PR」只翻状态**：放弃——v0.2 含实质决策改写（D2/D4/D5/D6/D8），
  状态与内容须同批复核，分开会产生「Accepted 但正文未修订」的窗口。
- **保留 v0.1 原文 + 追加修订节**（ADR-0036 定稿模式）：放弃——v0.1 含事实性
  错误（ADR-0025 死引用、错误心跳锚点、错误的「永久 409」论据），保留原文
  会造成第二真源；改以 §6「R→落点」对照表 + 版本记录保留决策链。
- **全量重写、丢 v0.1 痕迹**：放弃——对照表保留 R1–R19 与 D-1～D-6 的可追溯性。

## Verification

- 6 条事实更正逐条在 main 复核：`cron_scheduler.py:29` 3 天保留期
  （`run_retention_cleanup` 连 Job/StepTrace/租约/产物清理）、ADR-0025
  `retention|purge|保留期` 零命中、`test_host_identity.py:22` 后缀用例存在、
  `hosts.ts:24-25` + `HostsPage.tsx:107/:308` 删除调用点、`heartbeat.py:196-236`
  权威心跳（`agent/heartbeat.py:84` 客户端）、`job_timeout_config.py:86` 单源
  （R18 合销）；
- 14 面矩阵锚点抽样复核（`plan_dispatcher_sync.py:68`、
  `admission_pump.py:292-293,666-676`、`heartbeat.py:249-262,403`、
  `host_upgrade_gate.py:229-231`、`schemas/host.py:104`、`models/audit.py:22`
  等）；
- 文档级变更无测试面；`scripts/run_gates.py check:quick` 结果见 PR。

## Revisit

- 实现单拆分与「收口点 ↔ 测试文件」映射（synthesis §7 / ADR §5）；D-4 载体列名
  （`retire_alerted_at`）与具体类型以实现单为准。
- ADR-0037 的 README/DOC-MAP 登记本次一并补入（其状态仍 **Proposed**，待
  R02 联审，不随本单变更）。
