# ADR-0033 至 ADR-0051 与 #2546 定向复审落盘

Status: implemented
Class: process

## Decision

将本次发现写入独立的
[Markdown 审查报告](../../reviews/REVIEW_ADR0033_TO_0051_ISSUE2546_2026-09-23_ae232a3.md)，
记录完整基线、逐篇 ADR 状态、稳定发现 ID、证据和未验证边界。只改报告与本 Note，
不修改 ADR/权威设计/代码、不办理 Issue 或生产操作；#2546 Close 不等于 ADR-0033
全链落地或 S15 承诺全部实现。

## Alternatives

- 用 TSX 看板交付：不采纳；用户明确要求本地 Markdown。
- 直接修订 ADR-0033、ADR-0051 与门禁：不采纳；审查不代替 owner 的方向裁决，
  且 #3093/#3094 与 Phase 3 已有独立工作范围。
- 回写 R01–R15 总纲进度：不采纳；本次是跨域定向复审，不是假称完成新的逐区审查。

## Verification

- 对照取证时基线（`main`/`origin/main`/远端当时一致）
  `main@ae232a308ee6a46890accec810448c22f1fbdfc5` 的 ADR 索引、
  ADR 正文、实现与测试；用 `gh issue view` 只读核对 #2546/#3075/#3093/#3094
  及关联实施单，`git ls-remote` 核对远端基线。定稿后 main 前进至
  `512e61a8`（PR #3201/#3206），经 `git diff --name-only` 复验其间提交未触及报告
  引用文件，行号锚点仍有效。
- 本地两份 Markdown 的相对链接检查：0 个缺失；
  `.venv/bin/python -m tools.dev.check_governance_surface --self-test` 通过，
  `.venv/bin/python -m tools.dev.check_governance_surface --check` 通过（S1–S15、S5x）；
  `git diff --no-index --check` 对两份新增文件均未报告空白错误（退出码 1 表示有新增 diff）。
- Pending：全后端/Agent/前端测试、`check:quick`、生产 fleet 复验；此前 Note
  的 11 完成/37 跳过是历史现场记录，不是本轮重新验证。

## Revisit

- ADR-0033 D3 获 owner 分域澄清、#3093/#3094 关闭，或 S15③ 增 diff 联动时
  更新相应发现的状态与证据。
- ADR-0051 的 fleet 全 `strict`、一轮全 `package_active`、scan 改读 manifest
  及 Phase 3 验收均有证据后，另开新基线复审，不覆盖本次快照。
