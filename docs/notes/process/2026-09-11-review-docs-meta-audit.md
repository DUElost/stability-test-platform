# 有效审查文档集审计（元审查）落地

Status: implemented
Class: process

## Decision

- 新增 [`REVIEW_COVERAGE_AND_FIX_EFFECTIVENESS_2026-09-11_fdca41-audit.md`](../../reviews/REVIEW_COVERAGE_AND_FIX_EFFECTIVENESS_2026-09-11_fdca41-audit.md)：
  对 9 份有效审查稿（主干 6、队列 2、在途 1）的系统性只读审计——代码级抽验 10 项、跨文档
  计数口径对照、联合全集引用扫描、合规核查、A/B/C 可信度分级与 6 条处置建议。
- 审计确认：裁决链以 `7d4f85-convergence` 为准（CA-C03 证伪、Q2 条件未满足）；`85d793` 的
  F01–F04 经代码抽验成立且**尚未被吸收**；`IV` 的 backstop 断言属实但状态已更新（09-10 恢复）。
- 更正记录：`7d4f85` 归属由（初版审计误判的）opencode 更正为 **dsh**；规则固化
  「会话号在他库命中 ≠ 归属、归属看会话记录本体」。
- 本 PR 同时补齐 #1349 三稿（`4e188e` / `e16d6d` / `7d4f85-convergence`）的 DOC-MAP 登记；
  `85d793` / `705379-synthesis` 的登记行与在队 PR #1369 保持一致（同文重复，避免 FIFO 冲突）。

## Alternatives

- **只留在会话/不落文档**：不采纳——审计结论含可执行处置项与口径警示，需可引用记录。
- **并入 `7d4f85-convergence` 或改写他稿**：不采纳——汇聚稿是裁决层，本稿是**文档集质量审计**，
  两者职责不同；改写他稿超出授权。
- **对 9 稿逐份全文复核**：不采纳——按「关键断言抽验 + 引用/口径/合规全量扫描」取舍，
  未抽验部分已在文中声明局限。

## Verification

- 抽验均在只读命令下完成并附复现命令（§8）；未运行 pytest/Vitest/迁移，未改被审文档与业务代码。
- `check:quick` 7 gates、`git diff --check`、链接检查（本稿与被登记行）见 PR；
- 队列依赖：`85d793`/`705379-synthesis` 链接在 #1369（队列在前）合入后生效。

## Revisit

- `7d4f85` 命名/入库裁决（**已执行**）、`7d4f85-convergence` 断链修复（**已执行 #1385**）、85d793 F01/F02 转回归或残余单（待办）——完成后回写 §6。
- 计数口径表（§2）随综合轮口径归一后刷新；本稿所有数值为 2026-09-11 时点值。
- 若后续新增同题稿，按 §6-5 收敛冻结建议（仅增补、不改裁决基准）处理。
