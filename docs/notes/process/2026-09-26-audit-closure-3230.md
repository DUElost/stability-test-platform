# 第一性原理审计的订正与缺口并表

Status: implemented
Class: process

## Decision

在既有 09-25 主稿续写 §11，并将主稿及 09-23 容量基线登记进 DOC-MAP。
#3230 正文现有 G1–G9 保持原编号；09-25 最后一条评论新增的六项顺次映射为
G10–G15，并在正文记录旧号对照。历史评论保留原文，避免把当时证据改写成当前事实。
ADR-0052 D1–D5 的部署后六项验收另由 #3370 跟踪，因为 #3244 已随实现合入关闭。

## Alternatives

- 新增第三份平行审计稿：会让同一主题出现竞争入口，故续写已有主稿。
- 复用评论中的 G8/G9 覆盖正文编号：会使 #3316/#3317 的既有引用失去确定含义。
- 把 #3244 的代码关闭状态直接当实施验收：ADR §5 明确要求部署后真机同口径复跑。

## Verification

- `origin/main`、#3230 正文与五条评论、#3244/PR #3359、生产 release 和 Prometheus 均于 2026-09-26 只读复核。
- 文档本地链接检查三文件均 0 失效；#3230 正文规范行 G1–G15 连续且 15 行均有载体。
- `git diff --check` 通过。`check:quick` 首次因新 worktree 未安装 ESLint 停止；复用主检出的
  前端依赖后 17 gates 通过。`schema-at-head` 因未配置 `DATABASE_URL` WARN 跳过，
  不作为数据库迁移验证。
- 生产部署、真机中止、B1/B2、真实浏览器和异机恢复均未由本次文档变更代验。

## Revisit

#3370 复跑、#105/#106 容量阶梯或 #107 长跑得到新证据时，只在主稿后续章节追加订正；
#3230 的 G1–G15 归属或验收状态变化时同步更新该台账。
