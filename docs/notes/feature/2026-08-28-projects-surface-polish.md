# 2026-08-28 — 项目界面批次 C：登记簿收尾 + 详情页对齐

承接 08-27 设计评审批次 A/B 的遗留三处 + 用户反馈「还是比较粗糙」。
两个独立 PR：本篇覆盖 C1（登记簿收尾）与 C2（详情页对齐）。

## C1 决定
- 页顶三张大统计卡 → 单卡 KPI 带（divide-x 三格，在跑>0 数字转 success 色），
  与抽屉内 KPI 行同款形态。
- 型号表头说明句重写（删「不会从 HONOR-MLD 等回填标签推断」实现向措辞）；
  统计口径「未映射」改「待映射」。
- 表内平台原生 `<select>` → 同款 chip 组（全部/各平台，aria-pressed）。

## C2 决定
- 详情页 4 列 KPI 卡 → 同款 KPI 带（四格）；
- 删除与「当前归属设备型号」重复的静态 match_models 段（保留 live 查询行）；
- 设备/计划卡片内边距与字号对齐列表页密度。

## 放弃的备选
- 型号表整块移入抽屉：映射是列表页核心操作，保留在页面可折叠区内。
- KPI 带加图标井：三列小数据量下图标无信息增量，纯排版更克制。

## 验证
- vitest projects 套件 + 全仓绿；tsc/eslint 绿；线上验收并入终验。

## 何时重议
- 项目数 >50 时 chip 筛选与 KPI 带信息密度需复评。
