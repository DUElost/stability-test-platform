# 治理面常驻上下文瘦身评估（只读盘点，未改动任何文件）

- **状态**：Historical；常驻细节保留裁决已由
  [`2026-09-05-ai-harness-convention-baseline.md`](../notes/process/2026-09-05-ai-harness-convention-baseline.md)
  取代，本文保留当时审计证据
- **日期**：2026-08-27
- **性质**：**分析评估**——synthesis 维度 #7（CLAUDE.md 体量 🟡）的首笔兑现。产出降级候选清单与安全网协议，供逐项裁决；本文件本身不含对治理面文件的任何修改
- **方法**：`wc` 逐文件实测字节数 + 按 `^## ` 切段统计各段字节占比 → 对照各段自述的加载意图（"始终加载"/按需）与引用频率定性评级。token 数不做虚估，统一用字节口径
- **安全网现状**：L0 结构门禁已进 CI lint（首夜实战绿，run `33006328888`）；L1 行为 evals 12 条全绿（`tools/dev/run_gov_evals.py`，workers=2）。**任何裁剪动作的前置与后置协议见 §5**

---

## 1. 常驻集成本总表

常驻集定义 = 根 CLAUDE.md 及其全部 `@import` 链（当前 5 个文件），每个 AI 会话无差别全额加载：

| 文件 | 行 | 字节 | 占比 | 备注 |
|------|----|------|------|------|
| AGENTS.md | 196 | 21,900 | 39.3% | 多段有明确的自述加载意图 |
| DOC-MAP.md | 131 | 11,950 | 21.4% | 导航/索引元数据为主 |
| CLAUDE.md | 119 | 10,484 | 18.8% | `决策记录` 一段占全文 31.7% |
| plan-c 存储设计文档 | 212 | 8,415 | 15.1% | 设计史档案被整篇 import |
| cursor-rules.md | 87 | 2,991 | 5.4% | 低频主题被全员驻留 |
| **合计** | | **55,740** | 100% | |

CLAUDE.md 分段：架构不变量 1573 / 关键约定 613 / 状态机 985 / 方案C 728 / **脚本目录契约 1632** / 环境变量 567 / 开发陷阱 460 / **决策记录 3325**。
AGENTS.md 分段 Top：**scan/upload 契约 5390** / **Key conventions 4715** / Dev commands 2606 / 生产机调试约束 2442 / Production access 2109 / Key env vars 1835 / Test quirks 875 / 其余 <800。

## 2. 候选清单（A=零风险直接做；B=需配套裁决；C=先观察再议）

### A 级候选（预计共削减 ≈16.6KB，≈30% 常驻量）

| # | 动作 | 省 | 论证 |
|---|------|----|------|
| A1 | 取消 CLAUDE.md 对 [plan-c 存储设计](../design/2026-plan-c-storage-and-access.md) 的整篇 import，改为普通链接留在原段落 | −8,415 | 活体摘要（728B 的方案C段+存储角色别名行）保留在位；细粒度布局/历史修订是查阅型内容，动手写存储代码时按链接取即可。删除 import 行属 S1 规则的合法形态（整行移除而非行内化） |
| A2 | 同法取消 cursor-rules.md 的 import | −2,991 | 主题只在编辑 `.cursor/rules/**` 或讨论规则分层时相关；已在 `docs/development/` 目录内可发现。**需配套**：在 DOC-MAP 开发/运维索引补一行锚点（该文档目前不在任何索引表中） |
| A3 | `决策记录` 表各单元格压缩为一行短句 + 链接到对应 ADR 文件 | −2,200± | 现 ADR-0029/0030 两行单元格数百字，内容完整存在于 `docs/adr/` 且 DOC-MAP/adr README 已互链——三处重复中唯一该瘦的就是驻留版 |

### B 级候选（方向正确，但动的是全局导航基础设施，需单独裁决）

| # | 动作 | 省 | 论证与代价 |
|---|------|----|-----------|
| B1 | DOC-MAP 瘦身：把「设计文档索引」「开发/运维索引」「PRD/验收索引」的全描述表格迁往 [`docs/README.md`](../README.md)（hub，非自动加载），DOC-MAP 仅保留阅读顺序 + Living 审查一览（单行 hook 化） + 分层定义 | −7,000~9,000 | DOC-MAP 自述定位是「地图」，现正文 60% 是逐条描述——那是 hub 的职责。风险：两个消费方依赖（新人 onboarding 序列在其他文档中以相对路径引用；多份评审以 DOC-MAP 为登记簿）。动作前置条件：先在 README 中镜像建好目标表格，再移、再删，S2 断链检查兜底 |

### C 级候选（明确不动或等数据）

| # | 项 | 处置 | 理由 |
|---|----|------|------|
| C1 | AGENTS「scan/upload 跨进程契约」5.4KB | **保持常驻，攒 4 周使用样本再议** | 段落开头自述「控制面实现方在控制面，所以留在本文（始终加载）」是有意决策；懒加载化需要先确认跨进程排障场景的出现频率，拍脑袋降级可能造成 dedup 排障时的契约盲区 |
| C2 | AGENTS「Production access」2.1KB | **保持常驻** | 安全属性：把"凭据在哪找/别去哪找"固定在每次会话视野内，正是防越权试探的护栏；搬走省 2KB 却增加 agent 摸索敏感面的倾向 |
| C3 | AGENTS「Key conventions」4.7KB | 保持 | 2026-08 新近有意沉淀（PR 合入机制/CI 分层/注意力预算概述），是最近事故线的活契约 |
| C4 | CLAUDE「脚本目录契约」1.6KB | 保持 | ADR-0020 事故高频复发区，169 字节的警告密度换事故率值得 |

## 3. 与既有原则的相容性核查

- 「文档只写现状，不写变迁」：A1/A2 是删除 import 行，B1 是表格搬家——均不引入编年叙事；
- 薄适配层哲学（cursor-rules.md 自己写的）：本次反向运用——驻留层放指针，全量内容留惰性文件；
- Agent Note 制度：裁剪执行 PR 属非平凡变更（改变所有会话可见的知识面），届时附 note 记录备选与验证；
- 本文档即 synthesis D2「规划意图轻量承载」的自我实践：影响面/验证方式已随本文档落盘。

## 4. 一个反对意见的公平呈现（供裁决时权衡）

反方论点：「55KB 在现代模型上下文里不值一提，瘦身的真实收益不是 token 而是**注意力信噪比**——但这两者都会随模型升级自然改善」。回应：信噪比收益是真的且当下就有效（每段无关文本都是对关键不变量的稀释，本仓两次事故教会我们『常驻契约必须显眼』）；字节口径选最小可信单位，避免夸大。若你判断 ROI 不足，C 级清单本身就是「刻意不追」的底稿。

## 5. 执行协议（采纳后才有第 0 步）

```
0. 基线固化：python tools/dev/run_gov_evals.py 全绿留档（已有 12/12 可引用）
1. 按 A1→A2→A3 逐项提交（一项一 commit，便于二分回滚）
   每项之后：python3 tools/dev/check_governance_surface.py --check 必须绿
            （S1 保证 import 移除不留行内残迹；S2 保证新加链接不断）
2. B1 若采纳：先镜像 README 表格 → 迁移 → DOC-MAP 删重复 → 全链路 links 校验
3. 终验：run_gov_evals.py 重跑 12 条对照基线 —— 任一条转红即为契约丢失，
   回滚对应 commit
```

## 6. 待裁决项

1. A 级三项是否整体放行？（影响面已在 §2 列明）
2. B1 是否立项？（建议独立 PR，先读一眼 docs/README.md 现状）
3. C 级四项是否照单转为「刻意不追/缓议」登记？

## 7. 裁决与执行记录

| 项 | 裁决 | 执行 |
|----|------|------|
| A1 plan-c import 降级 | 放行 | ✅ #457（1e37a08） |
| A2 cursor-rules 降级+锚点 | 放行 | ✅ #457（3740c3b） |
| A3 决策记录单行化 | 放行 | ✅ #457（4d0e9cd） |
| C1–C4 显式保留登记 | 支持 | ✅ #457（4a512fe，入 synthesis 刻意不追续页） |
| B1 DOC-MAP 迁移 hub | 立项 | ✅ 本 PR：三索引表迁 README、S2 防护网扩覆盖 hub；**保守变体**——Living 登记簿原文驻留未 hook 化（防损伤多 agent 消费面），压缩留待使用数据支持时另议 |
| B1 后续：Living 登记簿 hook 化评估（2026-08-27 只读分析） | 结论：**不建议压缩，正式关闭该项** | 15 行 4,943B 占 DOC-MAP 68%，但为 B1 后唯一带完整描述的登记簿；12 个引用方全部文件级链接（S2 兜底零断链风险），风险集中在**检索性**——「独有贡献/resume ID/产出方」是多 agent 交叉汇总的检索键，省 ~2.2KB 不抵损失。与 skill 预约制同批执行，PR 见当时提交 |
| C1 预判：scan/upload 段样本回看（2026-08-27 只读） | 预判：**保持常驻正确**，4 周正式样本到期后若无意外即登记为终局 | 7–8 月两个月内 dedup/scan/upload 排障记录 7+ 条（notes 3 + issues #380/381/386/387），密集于 8 月下旬——懒加载化会给高频排障造成契约盲区，与当初保守立场一致 |
