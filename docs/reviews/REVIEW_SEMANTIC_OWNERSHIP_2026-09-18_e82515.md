# 跨 ADR 语义所有权层立项 —— 第三份独立评审（zcode）

- 性质：[#2546](https://github.com/DUElost/stability-test-platform/issues/2546) Mode C 独立评审稿；只读，不改任何 ADR / 代码 / 测试 / issue 结论区。
- Harness：zcode（会话后六位 `e82515`）。
- 基线：`origin/main = 497952c8`（2026-09-18，自记；立案基线 `cebde359` 之后已并入 #2604 等约 60+ commit，含 3 篇新 ADR）。
- F-2 / F-3 / F-4 数字为本稿在基线树内**全部重测**，未照抄 issue。
- 独立性声明：立案提示词本身含两轮外部咨询转述与评论区两份他稿摘要，无法不回望。本稿不引用任何他稿论证；与他稿同点的结论均给出本稿自己的 file:line 复测依据，同点处标注【收敛】（对 synthesis 是信号放大，不是第二票源）。

---

## 一、对三方案的总评

| 方案 | 总评 | 一句话理由 |
|---|---|---|
| A（owner 表 + README `归属域` 字段 + S15） | **Accept-with-nits**（两处阻断性改写，均可修） | 形态选对了（表+门禁，不建宪法层），但 S15 判据②引用的机制（S2）实测**不存在该能力且覆盖不到 ADR 面**，「以本文为准」条款与 DOC-MAP 现行条款**直接撞车**——两处都不改，A 会精确地长成它要治理的那类缺陷 |
| B（三层 Core Semantics + 全量 Inventory） | **Needs-revision**（可并入两条） | 第一步「从 48 篇抽全部名词」对着实测只有 **11–14 行权威声明、两组真冲突**的账开工，产出/成本比不成立；「一概念一 owner」判据句与五问决策树可并入 A |
| C（不立项，逐对收口） | **Needs-revision** | 评审窗口内（09-17→09-18）就有两例逐对收口**结构性够不到**的事实时态恶化（见 F-05、F-06）；C 的病不是收口太慢，是**那个面无任何机械判据** |

## 二、对 issue 事实的复验（全部自测）

| 项 | 结论 | 本稿证据（基线 `497952c8`） |
|---|---|---|
| X1 三口径 | **修正成立：实为「2 真声明 + 1 幻影引述」** | 真声明：`docs/adr/ADR-0021-script-content-alignment-gate.md:96`（`### D4 — 平台 DB 是脚本内容唯一权威`）、`ADR-0033-tool-kit-ecosystem-integration.md:121`（「DB script 目录…仍是**唯一运行时权威**」）。第三腿 `ADR-0021:297` 是**关联区引述**：「ADR-0020 —（`plan_snapshot.script_meta` 作为权威）」——而 `ADR-0020-plan-step-one-shot-migration.md` 全文 `script_meta` **0 命中**、`权威` **0 命中**；其实际内容是快照**隔离规则**（`ADR-0020:219`「都只从 `plan_snapshot` 读取…不得回读 live Plan 补值」），是「防回读」不是「称权威」。⇒ X1 的第三处不是双标，是**引述失真**（同疾病降一档，见 F-06） |
| X2 四层并存 | **确认；「无汇总」需限定** | `ADR-0028:44`（DLE「唯一权威记录」）、`ADR-0018:315`（`log_signal`「异常事件权威流」）逐字命中；`ADR-0033:36/44`（§1.1 行为/结构权威分家表）在场。汇总面**非零**：`ADR-0033:18`（v1.1 变更记录）与 `docs/adr/README.md:86`、`docs/DOC-MAP.md` ADR-0033 登记行都复述分家——真实缺陷是**汇总只存在于变更记录与索引描述里，无表行可引用**，不是「没有任何一处」 |
| X3 merge 归属三处 | **确认** | 提案 `docs/notes/architecture/2026-09-15-center-storage-and-merge-locus-proposal.md:11-12`（Decision 2「两项均属 ADR-0025 域，不新立 ADR」）vs 台账 `docs/notes/process/2026-09-15-device-log-flow-issue-backlog.md:247`（「I-13 merge 执行位置（ADR-0025 / ADR-0033 域）」）vs `ADR-0027:130`（v1.8 清单第 7 条解除路径「**B1** = 把 merge 迁到 worker/Agent（**归 ADR-0033**」）。B0 裁决在场且一致（台账 `:32`、提案 `:75`、`ADR-0027:168`），**归属口径仍三线** |
| F-0 纠偏 | **确认** | `ADR-0039` 对 `日志链/日志事件/log chain` 0 命中；`:3` 状态 Proposed。属 Script 域，判定不变 |
| F-1 窗口 | **确认且比 issue 写的更早一步**【收敛】 | 「已发布…不可原地修改或删除」在 `AGENTS.md:15`、位于 `## 总原则`（`:6`），**不在** `## 硬不变量`（`:23`）。S11 实测**整文件扫描**（`tools/dev/check_governance_surface.py:1292-1293` 传全文），但其 11 条锚（`:270-283`）无一条命中该句（唯一相关锚是 `default_params 不可原地修改`，对应 `AGENTS.md:32`）。⇒ 该句**今天整条删除都不会红**；「转 Accepted 当日过时」已是下界而非上界。旁证：`ADR-0039:16` 转述该句时同样误标为「`AGENTS.md` 硬不变量」——连当事 ADR 都记错了它的层级 |
| F-2 权威密度 | **逐字复现** | `grep -c 权威 docs/adr/ADR-*.md`：0033 **13**、0026 7、0034 6、0018 6、0021 4、0029 3、0028 1。密度最高在脚本/工具域，成立 |
| F-3 引用计数 | **结论更强，数字两口径均不可复现** | 本稿口径①`git grep -l <号> -- . ':!docs/adr' ':!docs/reviews'`：0020 **155** / 0025 **99** / 0028 **65** / 0033 **43**；口径②仅代码树（backend/frontend/tools/scripts）：66/63/35/9。issue 的 132/129/71/25 两者都对不上→**引用数不得进任何判据**（口径敏感）。「稳定外键」结论在全部口径下成立。S14 判据原文 `check_governance_surface.py:37-45`，其排除已发布脚本目录的注释在 `:729-734`（`CODE_SCAN_SKIP_PREFIXES`） |
| F-4 脚本域规模 | **逐字复现** | 34 族 / 179 版本目录 / 321 个 `.py` / 107,464 行 |
| F-5 分类学空洞 | **确认** | `ADR-0033` 全文 `刷机|flash` 0 命中；`flash_firmware` 22 + `flash_preflight` 5 = 27 个版本目录 |
| F-6 命名轴 | **确认，且需更新：该病已在被逐篇收口的路上** | `T0–T3` 在 ADR-0031 正文 23 处/附录 30 处；新事实：**`ADR-0045`（Accepted，09-17）已把风险分级对外词表统一为 `S/A/B` 并把翻译钉死在前端**——即「命名裁决」在本仓有刚发生的成功先例，且形态恰是「一表 + owner + 派生面翻译」，未建宪法层。`ADR-0047`（09-18 Proposed）标题即「预算**归属**与不变量」——归属类争议仍在新增，不是收敛期 |
| ADR 篇数 | **漂移** | 现存 **48** 篇（issue 写 45；`cebde359` 后两天内 +0045/0046/0047）。日增 ~1.5 篇 ⇒ 「只预填在抖的行」这条纪律比形态选择更决定成败 |

## 三、新发现（issue 与「不做」清单之外）

### F-05【阻断（可修），且独立于本单成立】`docs/adr` 不在 S2 覆盖内；两篇已合入 ADR 引用**幽灵文件**，门禁全绿

`check_governance_surface.py:168`：`LINK_TREES = ("docs/reviews", "docs/notes")`——断链检查覆盖树**不含 `docs/adr`**（`link_files` 显式清单也不含）。实测后果：

- `ADR-0044:9` 与 `ADR-0045:12` 都链接 `./ADR-0025-run-console-and-command-execution.md`，该文件名在**全部 git 历史中从未存在**（`git log --all --diff-filter=A -- 'docs/adr/ADR-0025-run-console*'` 空）；ADR-0025 实名只有 `ADR-0025-phase4-architecture-alignment.md`。
- `ADR-0045:12` 还断言「ADR-0025（v2.5 D13 风险趋势桶…）」：真实 ADR-0025 **无版本记录块、无 v2.5**；`### D13` 实际在 `ADR-0029:505` 且语义是「详情页换问题」；「风险趋势」字样全 `docs/adr` 只命中 ADR-0045 自己。⇒ 幽灵文件名 + 幽灵版本 + 幽灵节号，三合一，**Accepted、owner 已裁决、已合入**。
- 本稿在基线树内实跑 `python tools/dev/check_governance_surface.py`：**「[OK] 治理面结构检查通过（阻塞项全绿：S1–S14、S5x）」**——与上述事实同时成立。
- S14 拦不住它：其锚定面是**代码注释**里紧跟 ADR 号的 `vX.Y`（`:37-45`），头部关联行的括号版本不在扫描面。

**落点建议**（比「等 S15 立项」强在即时可修）：把 `docs/adr` 加入 `LINK_TREES` 是**一行改动**，合入当日即判红上述两链。这也是 F7 想要的现成验收事件：改判据→两行转红→修 ADR 关联→转绿，全程有机械真值。它不与本表判据冲突：链接存在性是**引用面**的仓库级判据，不需要概念白名单（见 F3/N3）。

### F-06【观察】「引述失真」是本单疾病的降档形态，且已在治理输入里复发

X1 第三腿（ADR-0021:297 对 ADR-0020 的括号转述与原文不符）与 F-05（ADR-0045 的转述三合一失真）是同一件事：**跨文档转述无判据**——被转述的原文可以说不存在的话，而所有门禁看的是「格式面」。咨询稿同类错误（F-0 的域归属、把 `:44` 读成「§44」、`#724 Track Index` 在 docs 面**检索不到**——全库仅 `docs/notes/bug-fix/2026-09-10-merge-budget-dual-platform-1085.md:48` 命中 `#724` 且语义无关）三次复发。**建议并入「输出与纪律」**：凡 owner 裁决引用跨文档表述，必须带**可解析锚 + 命中性检查**，不接受凭编号转述。（`#724` 在 issue 面可能有实义，本稿只陈述 docs 面不可复核——不构成对其余 N1 依据的伤害，N1 另有两条仓内前例，见下。）

### F-07【观察】「冲突时以本文为准」条款在仓内是**稀有且高资格**的形态，不可作为表头模板复制

实测全仓实质内容裁决条款仅 **3 处**：`execution-contract.md:3`（「唯一权威源…两者冲突时以本文为准**并回溯修订 ADR**」）、`2026-storage-roles-and-aliases.md:4`（「冲突时以本文 + 代码为准」）、`ADR-0026:102`（节内护栏「任何实现细节冲突时以本节为准」）。共同前提：**该文档本身就是操作规格**（env/角色表、执行协议、设计护栏）。另有两处全局条款占据「最终裁决」位：`docs/README.md:4` 与 `DOC-MAP.md:9`——「**冲突时以代码与测试为准**」。索引不承载操作规格，不满足前提；且 `AGENTS.md`「开始任务时」第 1 步强制所有 harness 从 DOC-MAP 路由，若索引再挂「以本文为准」，任何冲突场景都在两跳内出现**两个互相不服从的"为准"**。⇒ 这是 N1 必须胜出的机制性理由，不只是审美判断。

## 四、对候选方案与「不做」清单的表态

**A**：成立，带两处阻断改写——
1. **（F-05/F3 派生）判据②「file:line 不可解析 → 红，复用 S2」不可执行**：S2 实现（`:147-164`）只做 markdown 相对链接目标存在性，**不解析 `path:line`、刻意跳过代码块**，且如 F-05 所示连 `docs/adr` 都不扫。落点：S15 自带解析器，且锚**不裸用行号**——取 S11 先例（锚=原文子串，`:268`「改写措辞必须连锚一起改」）与 S5x 教训（`:17` 弱锚=假绿）：**锚 = 文件 + 结构化定位（节标题/表格行 key）或文本模式，命中 0 或 >1 都红**；行号只作人读提示不进判据。
2. **（F-07/N1）删「沿用 storage-roles 冲突条款」**，改挂 N1 完整形态（归属可裁决、内容零裁决、争议只改表一行）。
3. 【建议】判据③分期：`归属域` 字段**在才生效、不在不红**——现存 48 篇 0 篇有该字段，全量必填即落地当日全红。字段本身即补齐触发器（版本 bump 触碰 → 补该篇），不做大补齐。
4. 【建议】不做 2「膨胀上界用 S6 卡」需配动作才非空话：`RESIDENT_BUDGETS`（`:202-212`，enforcement `:228/:1280`）按路径登记，新表落 `docs/design/` 必须**同步加一行 dict**；建议给值 ≈(60 行/6 KB)，对齐表「只登记在抖的行」纪律。
5. 【建议】行级豁免标记：X1 第三腿、`ADR-0033:134`（备选方案区的「弃」选项原文）证明**引用面/历史面**必须可声明「非本域权威」，否则 S15 类判据第一轮就会被合法引述判红、随后被整表绕过——学 S14 #2249 放宽先例。

「不做」1–5 条全部赞成；其中第 1 条在 F-05 证据下应升格为正向条款（= N5），第 2 条是 A 存活的必要条件（F-4/篇数漂移证明膨胀速度）。

**B**：整体 Needs-revision。可并入 A 的两条：① 「每 canonical 概念恰一 owner」作为**表头判据句**（一行，不建层）；② 五问决策树按「不做」5 落 `ai_work.py declare` §3.4 与 issue 模板。拒绝第一步（全量名词抽取）：分母实测——全部 48 篇 ADR 的权威类声明共 11 行（窄口径）/14 行（宽口径），真冲突 2 组 + 层级缺表 1 组；为这个账抽取 48 篇，违背「不做」2 且产出差刚好制造它要防的第 5 份文档。

**C**：Needs-revision，但**逐对收口应保留为 A 的运行期路径**（表 = 收口的登记面，不是替代品）。C 不够的实测证据：① X3 在 #2189 完成「何时做」裁决**之后**归属仍是三线——逐对收口解时不解权；② F-05 两链与 F-06 引述失真发生在**立案与两份评审进行的同一窗口内**且门禁无感——说明「等下个 #530」的路线对新引入的失真零拦截；③ F-0：ADR-0021:297 这条引述错了一级的文本被立案方当作 X1 第三腿引用——没有表、没有锚检查，错误引述会进入治理输入并再生产。

## 五、N1–N6 表态

- **N1：支持改判（Ownership Authority only）。** 仓内前例（不取不可复核的 `#724`）：`ADR-0011:32`「上位/下位关系，不构成平行权威源」、`ADR-0033:36-44` 行为/结构权威分家表——**索引型分权在 Accepted ADR 里有现成形态**；`docs/adr/README.md:91`（ADR-0038 行「9 稿评审 synthesis + 人工裁决 D-1～D-6，#1557」）证明「归属争议 → issue 内 owner 人工裁决 → 只落结论一行」机制已跑通过一次。F-07 的条款碰撞分析给的是硬约束：保留原写法的 A 会**自身制造第一个跨文档冲突**。
- **N2：支持边行。** 我登记的边（各带实证）：① `merge ─执行位置→ 实例/宿主`（X3 三线实证）；② `ADR 号 ─映射→ 文件名/版本/节`（F-05/F-06 实证，owner=`adr/README` 主表 + 文件名本身，派生面=各 ADR 关联行，复议触发=S2/S15 锚红）；③ `Agent 安装 ─执行归属→ RunConsole`（ADR-0044 D1 已裁、作为「收口完成」样例行展示表的正向用途）。**判为伪需求**：`ScriptVersion ─bound_to→ PlanStep`——该边真源在 DB 与 `refs` 判据（`ADR-0039:68` + `scripts/check-script-version-immutability.py`），文字面裁决不改变它，登记即冗余。范围纪律照 N3：**只收「当前 ≥2 处文字互相不一致」的边**。
- **N3：接受白名单，但配一条仓库级判据防退化。** 「只检查自己写过的东西」的担忧对**归属裁决面**成立且无害（表外概念由 F1 路线的量化纪律兜底）；真正防退化的是把**引用面**做成全仓生效的非白名单判据——即 F-05 的 `LINK_TREES += docs/adr` 与 S15 锚解析器。白名单内查"谁是 owner"（人工裁决 + 表行唯一），白名单外查"引用是否指得对"（机械）。二者正交，合起来才覆盖 Y1/Y2 之外的失真面。
- **N4：采纳，且它正是 F5 的解。** Referential≠Semantic 划分与 S11 实测吻合（锚只测字符串在场）；据此**不建**「锚与 Accepted ADR 语义一致」类判据——「删除允许性」没有可判定谓词，建了就是第二个「红灯不可修死结」（S14 注释 `:729-734` 自述过的坑）。
- **N5：升格为正向条款，采纳。** F-05 给出代价实证：幽灵引用三处失真已在 Accepted 面发生——正因为「处置不含 Rename/Merge/Renumber」目前**不成文**。`Scoped` 前例本稿另补：`ADR-0033 v1.2 §5`（`:160-164`，D2/D3 收窄适用范围而语义不变）与 `ADR-0029` D1/D4/D5… 挂起条款（`adr/README.md:81`，「原文保留、复议触发条件」）——后者就是表里「复议触发器」列的同形先例。
- **N6：五层可用，但写进**表头注释**而非各文档。** ADR-0045 证明「词表 + 翻译留前端」是收口命名冲突的既成模式；本表头部一段（≤5 行）声明五层即可。给 `Domain Authority` 层补一条实测注记：`execution-contract.md:3` 现挂的是仓内**最强**冲突条款（「以本文为准并回溯修订 ADR」）——它的资格来自"该文即操作规格"（F-07），**不可**被本表援引为自我授权先例，五层文本里应写明这一句，防止第一份后续文档抄条款而不是抄资格。

## 六、F1–F7 逐条

- **F1（量化判据）**：分母=48 篇 ADR 全部权威声明 11–14 行；真冲突 2 组（X1/X3）+ 层级缺表 1 组（X2）+ 窗口内新发失真 2 例（F-05/F-06）；「就地写清」路线（ADR-0011:32 式）的成立前提是**双方在同一篇内有互指**——X3 三方横跨 2 篇 ADR + 2 篇台账，X1 第三腿本身就是失真引述，就地写清无落点。⇒ **一张 8–12 行表 + 2 条机械判据有正收益；全量 inventory 没有。** 量化上界：若表的行数 > 2×(全仓权威声明行数 14)，即越界（B 化），触发 S6 预算红。
- **F2（边界四分）**：`DOC-MAP`＝文件粒度路由 + 权威/归档二分（`:115` 起）；`本表`＝概念/关系粒度 owner 索引（**只答"谁拥有定义权"**）；`AGENTS.md`＝行为红线（S11 锚定）；`execution-contract`＝执行语义操作规格。supersede 链不存在也不该存在：内容冲突走 `DOC-MAP:9`/`docs/README:4` 既有的「代码与测试为准」，本表**无权也不需**插队。表的列硬边界：无「解释」列，一切解释以锚指向 owner 文档。
- **F3（假阳性面）**：①「≥2 owner」若从**散文**推导 owner 必然 FP（X2 四层全对，字面同形）⇒ 判据只读表行，永不解析散文（与 N3 一致）；② 锚强度 FP/假绿面：裸 `file:line` 是**假绿**面（S5x 教训），文本模式是歧义面 ⇒ 命中 0 或 >1 都红 + 行级豁免标记（第四节建议 5）；③ 引用面 vs 声明面（`ADR-0033:134` 备选区「弃」选项、`ADR-0021:297` 引述行都是 X1 证据本体）⇒ 豁免标记必须带理由，学 S14 #2249。
- **F4（三处单行裁决）**：
  - **X1**：owner=`ADR-0033 D3`（运行时，`:121/:164`）；`ADR-0021 D4`（`:96`）收窄措辞为「发布/对齐门禁面的内容权威」——两条本就分层，**改措辞即可，不动决策**；`:297` 括注改为「快照隔离（ADR-0020），非权威源」——这不是裁决是**勘误**。
  - **X2**：非冲突，一行组四列登记（0028 DLE / 0018 log_signal / 0032 分区与 merge 行为 / 0025 存储模型），派生面点名 `log_observation`；**不改**四篇任何措辞。
  - **X3**：现状 owner=`ADR-0027 v1.8 第 7 条`（`:118/:130`，唯一同时说清现状 flock 绑定与 B1/B2 解除归属的）；B1 触发后该边改指 `ADR-0033`（其自身 `:130` 已含此指针）；提案 `:11-12` 的「两项均属 0025 域」收窄为「**存储布局面**（I-12）归 0025」，台账 `:247` 标题随之。与 B0 完全相容（B0 裁"何时"，本裁决钉"谁定义"）。
- **F5（时序）**：三步，不二选一：① **立即给 `AGENTS.md:15` 补 S11 锚**（一行、与本单成立与否无关；F-1 行已证该句今天无保护）；② ADR-0039 转 Accepted 当日**同 PR**改写该句（锚在场后 S11 才把"改措辞连锚一起"变成机械义务）；③ 语义一致判据不建（N4）。
- **F6（补齐策略）**：字段即触发器（第四节 A-3），不做一次性 48 篇补齐；新 ADR 自模板起写（`adr/README.md:25-33` 模板加一行 `归属域：semantic-ownership §N`）。与 ADR-0034 衔接：本表是单文件、走 `declare` + 共享元文件同一时间单 Execution 规则即可，无需新机制。顺手项：模板「目标里程碑：M1 | M2 | M3」（`:30`）与实际在用的 M4–M7 漂移——触碰模板时一并修，这也是「模板面无机械检查」的又一个零成本证据。
- **F7（验收可测性）**：三条可判定事件：① `LINK_TREES += docs/adr` 合入当日 F-05 两链判红、修复后转绿（幽灵从仓内消失）——**评审期内即有第一次全周期演示**；② 表内 X1/X2/X3 各行锚「命中恰 1」为绿，三处双标在治理面收敛为表行；③ 下一条 Y1/Y2 式双标单在**关单前**表内出现对应行（挂 `finish` 判据或 issue 模板一行，不新增常驻文本）——若连续出现"修完未登记"，即证明表无牵引力，按 #2108 路线做收益复核并允许本层降级为人工清单（代价：F-05/F-06 类失真回到无判据状态，此为写明价的退出条款）。

## 七、验证记录

- 已运行：`git fetch` + 基线自记（`497952c8`）；`ai_work.py status/declare`；本稿第二节全部 grep/计数命令（在 `497952c8` 工作树内）；`git log --all --diff-filter=A`（幽灵文件历史核验）；`python tools/dev/check_governance_surface.py`（基线树内全绿，F-05 证据）。
- 未运行：pytest / vitest / 其余门禁子集——本稿为 docs-only 只读评审，无代码改动；`check:quick` 的完整跑留给 PR CI 判定。
- 未做：全库（非 `docs/adr`）概念审计；`REVIEW_SEMANTIC_OWNERSHIP_2026-09-17_004377.md` 与评论区他稿全文未读（仅 issue 内自带的摘要段不可回避，见独立性声明）。
