---
description: Agent 运行时 — Watcher、AEE、scan/upload、ADB
globs: backend/agent/**/*
alwaysApply: false
---

# Agent Runtime

修改 Agent 运行时代码前按需读取：

- `backend/agent/CLAUDE.md`：Agent 侧 scan/upload 与热刷新边界；
- `backend/agent/aee/CLAUDE.md`：AEE/UNISOC 采集链和平台路由；
- `docs/design/2026-scan-upload-merge-contract.md`：控制面跨进程归档契约；
- `docs/development/testing.md`：生产机测试安全边界；
- `AGENTS.md`：脚本执行与跨层硬不变量；
- `CLAUDE.md`：存储角色和领域文档路由；
- `docs/design/04-agent.md`：Agent 模块设计。

环境变量的完整现状以 `backend/agent/.env.example` 和开发文档为准。本规则不复制默认值、
命令参数、平台白名单或数据流摘要。
