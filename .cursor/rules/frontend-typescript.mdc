---
description: 前端 TypeScript/React — 类型同步与测试
globs: frontend/**/*.{ts,tsx}
alwaysApply: false
---

# Frontend

修改前端 TypeScript/React 前按需读取：

- `AGENTS.md`：前后端 schema 同步等跨层不变量；
- `CLAUDE.md`：领域文档路由；
- `docs/development/testing.md`：前端测试 quirks 与验证命令；
- `docs/development/repository-workflow.md`：CI 与集成约定；
- `frontend/package.json`：当前可执行脚本；
- `docs/design/03-frontend.md`：路由、API client 与页面架构。

涉及后端 schema、Watcher 或 PlanRun 协议时，同时读取对应后端 schema 与设计文档。
本规则不复制脚本名、超时值或数据流摘要。
