---
description: 后端 Python — Pydantic、DB、pytest
globs: backend/**/*.py
alwaysApply: false
---

# Backend Python

修改后端 Python 前按需读取：

- `AGENTS.md`：Pydantic、表名和跨层硬不变量；
- `CLAUDE.md`：状态机与领域文档路由；
- `docs/development/testing.md`：当前测试命令与隔离数据库要求；
- `docs/design/2026-scan-upload-merge-contract.md`：跨进程归档契约；
- 对应 `docs/design/` 文档：模块和协议细节。

本规则只负责按路径引导，不保存命令参数、环境变量默认值或实现摘要，避免与权威文档
及代码形成第二套事实源。
