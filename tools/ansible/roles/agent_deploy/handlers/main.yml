- name: daemon reload
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart agent service
  become: true
  ansible.builtin.systemd:
    name: "{{ agent_service_name }}"
    state: restarted
