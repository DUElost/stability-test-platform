# Agent runtime dependencies
# Installed by install_agent.sh into /opt/stability-test-agent/venv

# HTTP client (heartbeat / job claim / step trace upload / lease renewal)
requests>=2.28,<3.0

# .env loader (soft dep: agent main.py wraps in try/except)
python-dotenv>=1.0,<2.0

# SocketIO sync client — connects /agent namespace for real-time
# step_log / step_update / heartbeat broadcast.
# [client] extra pulls in python-engineio + websocket-client.
# Without this the agent silently degrades: HTTP works but no log stream.
python-socketio[client]>=5.10,<6.0
